# 🚀 Деплой семейного кошелька

## Быстрый старт

### 1. Коммит и пуш изменений

```bash
# В локальной директории проекта
git add .
git commit -m "feat: добавлен семейный кошелек"
git push origin main
```

### 2. Автоматический деплой через GitHub Actions

После пуша в `main` ветку:
1. GitHub Actions автоматически запустит деплой на VPS
2. Бот пересоберется и перезапустится
3. Миграция БД выполнится автоматически

**Следить за процессом:**
- GitHub → Actions → Deploy to Production
- Или через SSH: `docker-compose logs bot -f`

### 3. Проверка после деплоя

```bash
# Подключиться к VPS
ssh kimster@your-vps-ip

# Перейти в директорию проекта
cd /opt/money_bot

# Проверить статус контейнеров
docker-compose ps

# Проверить логи бота
docker-compose logs bot --tail=50

# Найти строку о миграции
docker-compose logs bot | grep "Миграция"

# Должно быть:
# ✅ Миграция БД успешно завершена!
```

## Ручной деплой (если нужно)

### Вариант 1: Через GitHub Actions (рекомендуется)

1. Перейти в GitHub → Actions
2. Выбрать "Deploy to Production"
3. Нажать "Run workflow"
4. Выбрать ветку `main`
5. Нажать "Run workflow"

### Вариант 2: Полностью вручную на VPS

```bash
# 1. Подключиться к VPS
ssh kimster@your-vps-ip

# 2. Перейти в директорию проекта
cd /opt/money_bot

# 3. Остановить текущие контейнеры
docker-compose down

# 4. Обновить код
git fetch origin main
git reset --hard origin/main

# 5. Пересобрать и запустить
docker-compose up -d --build

# 6. Проверить логи
docker-compose logs bot --tail=50

# 7. Проверить статус
docker-compose ps
```

## Что проверить после обновления

### ✅ Чеклист

- [ ] Контейнеры запущены (docker-compose ps)
- [ ] Нет ошибок в логах (docker-compose logs bot --tail=50)
- [ ] Миграция прошла успешно (есть строка "✅ Миграция БД успешно завершена!")
- [ ] Бот отвечает на команды
- [ ] Команда `/start` работает
- [ ] Команда `/balance` показывает семейный баланс
- [ ] Команды `/link` и `/family` доступны

### Команды для проверки

```bash
# Проверка статуса
docker-compose ps | grep -q "Up" && echo "✅ Контейнеры запущены" || echo "❌ Проблема с контейнерами"

# Проверка ошибок
docker-compose logs bot --tail=100 | grep -i "error" && echo "⚠️ Есть ошибки" || echo "✅ Нет ошибок"

# Проверка миграции
docker-compose logs bot | grep -q "Миграция БД успешно завершена" && echo "✅ Миграция OK" || echo "⚠️ Миграция не найдена"

# Проверка подключения к БД
docker-compose logs bot | grep -q "База данных инициализирована" && echo "✅ БД подключена" || echo "❌ Проблема с БД"
```

## Тестирование функционала

### Тест 1: Проверка одиночного пользователя

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Проверьте что показывается баланс и список команд
4. Отправьте `/income` и введите сумму (например: 1000)
5. Проверьте что баланс обновился
6. Отправьте `/balance` - должен показать семейный баланс
7. Отправьте `/history` - должна быть одна транзакция

### Тест 2: Проверка семейной привязки

**Супруг 1:**
1. Отправить `/link`
2. Получить код (например: `ABC123`)
3. Отправить код второму супругу

**Супруг 2:**
1. Запустить бота `/start`
2. Отправить `/join`
3. Ввести код `ABC123`
4. Проверить что балансы объединились

**Оба супруга:**
1. Отправить `/balance` - должен быть одинаковый баланс
2. Отправить `/family` - должны быть оба участника
3. Один добавляет `/income 500`, второй проверяет `/balance` - баланс должен обновиться у обоих
4. Отправить `/history` - должны быть транзакции обоих с указанием кто добавил

## Откат изменений

### Если что-то сломалось

```bash
# 1. Остановить бота
docker-compose down

# 2. Восстановить из бекапа (если есть)
docker exec -i money_bot_db psql -U postgres -d money_bot < /backups/money_bot_$(date +%Y%m%d).sql

# 3. Откатить код на предыдущий коммит
git log --oneline  # Найти хеш предыдущего коммита
git reset --hard <previous-commit-hash>

# 4. Запустить старую версию
docker-compose up -d --build

# 5. Проверить
docker-compose logs bot --tail=50
```

### Если нужно откатить только БД

```bash
# Подключиться к контейнеру PostgreSQL
docker exec -it money_bot_db psql -U postgres -d money_bot

# Выполнить SQL для отката:
DROP TABLE families CASCADE;
ALTER TABLE users DROP COLUMN family_id;
ALTER TABLE users ADD COLUMN current_balance NUMERIC(15, 2) DEFAULT 0.0;
ALTER TABLE transactions DROP COLUMN family_id;
ALTER TABLE transactions DROP COLUMN user_name;

# Восстановить балансы из транзакций
UPDATE users u
SET current_balance = (
    SELECT COALESCE(
        SUM(CASE 
            WHEN t.transaction_type = 'income' THEN t.amount
            WHEN t.transaction_type = 'expense' THEN -t.amount
            ELSE 0
        END),
        0
    )
    FROM transactions t
    WHERE t.telegram_id = u.telegram_id
);
```

## Мониторинг

### Постоянный мониторинг логов

```bash
# Следить за логами в реальном времени
docker-compose logs bot -f

# Следить только за ошибками
docker-compose logs bot -f | grep -i error

# Следить за действиями пользователей
docker-compose logs bot -f | grep -E "(income|expense|link|join)"
```

### Проверка размера БД

```bash
# Размер БД
docker exec money_bot_db psql -U postgres -d money_bot -c "SELECT pg_size_pretty(pg_database_size('money_bot'));"

# Количество пользователей
docker exec money_bot_db psql -U postgres -d money_bot -c "SELECT COUNT(*) FROM users;"

# Количество семей
docker exec money_bot_db psql -U postgres -d money_bot -c "SELECT COUNT(*) FROM families;"

# Количество транзакций
docker exec money_bot_db psql -U postgres -d money_bot -c "SELECT COUNT(*) FROM transactions;"
```

## Бекапы

### Создать ручной бекап перед обновлением

```bash
# Создать директорию для бекапов
mkdir -p /opt/money_bot/backups

# Создать бекап
docker exec money_bot_db pg_dump -U postgres money_bot > /opt/money_bot/backups/before_family_wallet_$(date +%Y%m%d_%H%M%S).sql

# Проверить что файл создан
ls -lh /opt/money_bot/backups/
```

### Восстановить из бекапа

```bash
# Остановить бота
docker-compose down

# Удалить текущую БД
docker volume rm money_bot_postgres_data

# Запустить только postgres
docker-compose up -d postgres

# Подождать пока postgres запустится
sleep 10

# Восстановить из бекапа
docker exec -i money_bot_db psql -U postgres -d money_bot < /opt/money_bot/backups/before_family_wallet_XXXXXX.sql

# Запустить бота
docker-compose up -d bot
```

## Troubleshooting

### Проблема: Миграция не выполняется

**Симптомы:** В логах нет строки "Миграция БД успешно завершена"

**Решение:**
```bash
# Проверить что таблица families создана
docker exec money_bot_db psql -U postgres -d money_bot -c "\dt"

# Если families нет - пересоздать БД
docker-compose down
docker volume rm money_bot_postgres_data
docker-compose up -d
```

### Проблема: Ошибка "family_id не существует"

**Симптомы:** `ERROR: column "family_id" does not exist`

**Решение:**
```bash
# Пересоздать БД с нуля
docker-compose down
docker volume rm money_bot_postgres_data
docker-compose up -d --build
```

### Проблема: Баланс не синхронизируется между супругами

**Симптомы:** У супругов разный баланс

**Решение:**
```bash
# Проверить что у них одинаковый family_id
docker exec money_bot_db psql -U postgres -d money_bot -c "SELECT telegram_id, family_id FROM users;"

# Если family_id разные - нужно перепривязать
# Один из супругов отправляет /link, второй /join с кодом
```

### Проблема: Команды /link и /family не работают

**Симптомы:** Бот не отвечает на новые команды

**Решение:**
```bash
# Проверить версию кода
cd /opt/money_bot
git log --oneline -1

# Убедиться что последний коммит с семейным кошельком
git pull origin main
docker-compose down
docker-compose up -d --build
```

## Контакты и поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose logs bot --tail=100`
2. Проверьте статус: `docker-compose ps`
3. Проверьте чеклист выше
4. Создайте issue в GitHub с описанием проблемы и логами

---

**Удачного деплоя! 🚀**

