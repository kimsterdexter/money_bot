# 💰 Money Bot - Бот для учета финансов

Простой Telegram-бот для ежедневного трекинга доходов и расходов.

## 🚀 Быстрый старт (2 команды)

```bash
# 1. Настрой токен
cp env.example .env
# Отредактируй .env и вставь BOT_TOKEN

# 2. Запусти всё
docker-compose up -d --build
```

**Готово!** Бот работает. Найди его в Telegram → `/start`

---

## 🚀 Функционал

- ✅ Запись пополнений счета
- ✅ Запись расходов
- ✅ Просмотр текущего баланса
- ✅ История транзакций
- ✅ Ежедневные напоминания (утром о пополнениях, вечером о расходах)
- ✅ Хранение данных в PostgreSQL

## 📋 Требования

### Способ 1: С Docker (рекомендуется)
- Docker и Docker Compose
- Telegram Bot Token (получить у [@BotFather](https://t.me/botfather))

### Способ 2: Без Docker
- Python 3.11+
- PostgreSQL 14+
- Telegram Bot Token

## 🛠 Установка и запуск

### Способ 1: Docker (рекомендуется) 🐳

**Требования:** Docker и Docker Compose

#### 1. Получить токен бота

Получи токен у [@BotFather](https://t.me/botfather) в Telegram

#### 2. Настроить .env

```bash
cp env.example .env
# Отредактируй .env и вставь свой BOT_TOKEN
```

#### 3. Запустить всё одной командой

```bash
docker-compose up -d --build
```

Готово! Бот работает.

**Проверка:**
```bash
docker-compose ps        # Статус контейнеров
docker-compose logs bot  # Логи бота
```

---

### Способ 2: Локально без Docker 🐍

**Требования:** Python 3.11+, PostgreSQL (можно через Docker)

#### 1. Получить токен бота (см. выше)

#### 2. Создать виртуальное окружение

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r backend/requirements.txt
```

#### 3. Настроить .env

```bash
cp env.example .env
# Отредактируй .env
```

#### 4. Запустить PostgreSQL

```bash
# Вариант A: через docker-compose (только БД)
docker run -d --name money_bot_db \
  -e POSTGRES_DB=money_bot \
  -e POSTGRES_PASSWORD=postgres \
  -p 5432:5432 \
  postgres:16-alpine

# Вариант B: локальный PostgreSQL
# Установи PostgreSQL и создай БД money_bot
```

#### 5. Запустить бота

```bash
python -m backend.bot.main
```

Бот запустится и начнет принимать команды!

## 📱 Использование

### Команды бота:

- `/start` - Начать работу с ботом
- `/help` - Справка по командам
- `/income` - Записать пополнение счета
- `/expense` - Записать расход
- `/balance` - Показать текущий баланс
- `/history` - История последних 10 транзакций
- `/cancel` - Отменить текущую операцию

### Ежедневные напоминания:

- **09:00** (утром) - Напоминание записать пополнения за вчерашний день
- **20:00** (вечером) - Напоминание записать расходы за сегодня

Время можно изменить в файле `.env`:

```env
DAILY_INCOME_TIME=09:00
DAILY_EXPENSE_TIME=20:00
```

## 🗄 Структура базы данных

### Таблица `users`:
- `telegram_id` - ID пользователя в Telegram
- `username` - Username пользователя
- `first_name`, `last_name` - Имя и фамилия
- `current_balance` - Текущий баланс
- `created_at`, `updated_at` - Временные метки

### Таблица `transactions`:
- `id` - ID транзакции
- `telegram_id` - ID пользователя
- `transaction_type` - Тип (income/expense)
- `amount` - Сумма
- `description` - Описание
- `created_at` - Время создания

## 🔧 Управление ботом

### С Docker:

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Перезапуск
docker-compose restart

# Только бота
docker-compose restart bot

# Логи (live)
docker-compose logs -f bot

# Пересборка после изменений
docker-compose up -d --build
```

### Без Docker:

```bash
# Запуск
python -m backend.bot.main

# Остановка
# Ctrl+C в терминале
```

## 📊 Логи

Логи сохраняются в файл `bot.log` и выводятся в консоль.

## 🧪 Тестирование

### Быстрая проверка работоспособности:

1. Запусти бота
2. Найди бота в Telegram (по username, который указал @BotFather)
3. Отправь `/start`
4. Проверь команды:
   - `/income` → введи `1000` → проверь, что баланс увеличился
   - `/expense` → введи `500` → проверь, что баланс уменьшился
   - `/balance` → проверь правильность баланса (должен быть 500)
   - `/history` → проверь, что обе транзакции отображаются

## ⚠️ Важные замечания

1. **Бот должен быть запущен постоянно** для отправки ежедневных напоминаний
2. Для продакшена рекомендуется:
   - Использовать systemd или supervisor для автозапуска
   - Настроить резервное копирование БД
   - Использовать webhook вместо polling
   - Добавить rate limiting
3. Храни `.env` в секрете (он уже в `.gitignore`)

## 🐛 Troubleshooting

### Бот не запускается:
```bash
# Проверь статус контейнеров
docker-compose ps

# Проверь логи бота
docker-compose logs bot

# Проверь .env файл
cat .env | grep BOT_TOKEN
```

### База данных недоступна:
```bash
# Перезапусти контейнеры
docker-compose restart

# Или пересоздай
docker-compose down
docker-compose up -d
```

### Изменения в коде не применяются:
```bash
# Пересобери образ
docker-compose up -d --build
```

### Напоминания не приходят:
- Проверь настройку TIMEZONE в `.env`
- Проверь настройку времени DAILY_INCOME_TIME и DAILY_EXPENSE_TIME
- Убедись, что контейнер бота работает: `docker-compose ps`
- Проверь логи: `docker-compose logs bot`

### Полная очистка и перезапуск:
```bash
# Удалить всё (включая данные БД!)
docker-compose down -v

# Пересоздать с нуля
docker-compose up -d --build
```

## 📝 Следующие шаги (опционально)

- [ ] Добавить веб-интерфейс на React для просмотра аналитики
- [ ] Добавить экспорт данных в Excel/CSV
- [ ] Добавить графики и статистику
- [ ] Добавить категории расходов (если понадобится)
- [ ] Добавить множественные счета
- [ ] Добавить валюты

---

## 🏭 Production Deploy

Для деплоя на продакшен-сервер смотри **[PRODUCTION.md](PRODUCTION.md)**

Быстрый деплой:
```bash
./deploy.sh
```

---

## 📂 Структура файлов

```
money_bot/
├── backend/                    # Код бота
│   ├── bot/
│   │   ├── main.py            # Точка входа
│   │   ├── handlers.py         # Команды бота
│   │   └── scheduler.py        # Планировщик напоминаний
│   ├── db/
│   │   ├── database.py         # Подключение к БД
│   │   └── models.py           # ORM модели
│   ├── config.py               # Конфигурация
│   └── requirements.txt        # Зависимости Python
├── tests/                      # Тесты
│   └── test_smoke.py
├── Dockerfile                  # Docker образ бота
├── docker-compose.yml          # Оркестрация (бот + БД)
├── .dockerignore               # Исключения для Docker
├── deploy.sh                   # Скрипт деплоя
├── run_tests.sh                # Скрипт тестов
├── env.example                 # Пример .env
├── README.md                   # Документация
├── QUICKSTART.md               # Быстрый старт
└── PRODUCTION.md               # Production deploy guide
```

---

**Автор**: Kimster  
**Стек**: Python, aiogram, PostgreSQL, Docker, Docker Compose

