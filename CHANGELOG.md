# 📝 Changelog - Семейный кошелек

## Версия 2.0.0 - Семейный кошелек (08.12.2025)

### ✨ Новые функции

#### Семейный кошелек
- **Общий баланс для супругов** - теперь доходы и расходы учитываются на одном счете
- **Привязка членов семьи** через код (команда `/link`)
- **Отображение автора транзакции** - в истории видно кто добавил доход/расход
- **Автоматическое объединение балансов** при привязке
- **Просмотр участников семьи** (команда `/family`)

#### Новые команды
- `/link` - создать код для привязки супруга/супруги (действует 10 минут)
- `/join` - присоединиться к семье по коду
- `/family` - показать участников семьи и общий баланс

### 🔄 Изменения

#### База данных
- Добавлена таблица `families` (id, name, current_balance, created_at, updated_at)
- В таблицу `users` добавлен `family_id` (внешний ключ)
- Из таблицы `users` удален `current_balance` (теперь хранится в `families`)
- В таблицу `transactions` добавлены:
  - `family_id` - к какой семье относится транзакция
  - `user_name` - имя пользователя который добавил транзакцию

#### Логика работы
- При первом запуске для пользователя автоматически создается семья
- Все транзакции привязываются к семье, а не к конкретному пользователю
- Баланс показывается семейный, а не личный
- История транзакций общая для всей семьи
- Напоминания получают все члены семьи

### 🔧 Технические изменения

#### Измененные файлы
```
backend/db/models.py          # Новая модель Family, изменения в User и Transaction
backend/bot/handlers.py       # Логика семейного кошелька и команды привязки
backend/bot/scheduler.py      # Показ семейного баланса в напоминаниях
backend/db/database.py        # Автоматическая миграция при init_db()
backend/db/migrate.py         # НОВЫЙ - скрипт миграции данных
```

#### Добавленные файлы
- `FAMILY_WALLET.md` - Подробная инструкция по использованию
- `CHANGELOG.md` - Этот файл

### 📦 Миграция данных

**ВАЖНО:** Миграция выполняется **автоматически** при первом запуске обновленной версии!

Что происходит:
1. Для каждого существующего пользователя создается семья
2. Баланс пользователя переносится в баланс семьи
3. Все транзакции привязываются к соответствующим семьям
4. Добавляется информация об авторе транзакции

**Данные не теряются!** Все транзакции и балансы сохраняются.

### 🚀 Как обновить

#### На VPS (Production)

```bash
# Подключаемся к VPS
ssh your-server

# Переходим в директорию проекта
cd /opt/money_bot

# Останавливаем бота
docker-compose down

# Обновляем код
git pull origin main

# Пересобираем и запускаем
docker-compose up -d --build

# Проверяем логи
docker-compose logs bot --tail=50
```

#### Локально (Development)

```bash
# Останавливаем контейнеры
docker-compose down

# Обновляем код
git pull

# Пересобираем
docker-compose up -d --build

# Проверяем
docker-compose logs bot --tail=50
```

### ⚠️ Важные заметки

1. **Первый запуск может занять дольше обычного** - выполняется миграция данных
2. **Проверьте логи** после обновления: `docker-compose logs bot --tail=50`
3. **Старые данные сохранятся** - каждый пользователь получит свою семью с прежним балансом
4. **Резервная копия БД** создается автоматически через `scripts/setup_backups.sh`

### 🔍 Проверка после обновления

```bash
# 1. Проверить что контейнеры запущены
docker-compose ps

# 2. Проверить логи на ошибки
docker-compose logs bot --tail=50 | grep -i error

# 3. Проверить что миграция прошла успешно
docker-compose logs bot | grep "Миграция БД"

# Должно быть:
# ✅ Миграция БД успешно завершена!
```

### 🐛 Откат в случае проблем

Если что-то пошло не так:

```bash
# 1. Остановить бота
docker-compose down

# 2. Восстановить БД из бекапа
# (если настроили автоматические бекапы)
docker exec -i money_bot_db psql -U postgres -d money_bot < /backups/latest.sql

# 3. Откатиться на предыдущую версию кода
git checkout <previous-commit-hash>
docker-compose up -d --build

# 4. Или откатить изменения в БД вручную через psql
docker exec -it money_bot_db psql -U postgres -d money_bot
# ... SQL команды для отката ...
```

### 📊 Совместимость

- **Python**: 3.12+
- **PostgreSQL**: 13+
- **aiogram**: 3.x
- **SQLAlchemy**: 2.x
- **Docker**: 20.10+
- **Docker Compose**: 2.x

### 🎯 Roadmap

Планируемые функции:
- Отвязка от семьи
- Переименование семьи
- Приглашение по username вместо кода
- Статистика по членам семьи (кто сколько тратит/пополняет)
- Уведомление второго супруга при добавлении транзакции первым
- Категории расходов (еда, транспорт, развлечения)
- Экспорт истории в Excel/CSV

---

## Версия 1.0.0 - Первый релиз

### Базовый функционал
- Учет доходов и расходов
- Личный кошелек для каждого пользователя
- История транзакций
- Ежедневные напоминания
- Docker контейнеризация
- Деплой через GitHub Actions

